components:
  requestBodies:
    RequestBody1:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Foo'

  schemas:
    Foo:
      type: object
      properties:
        foo:
          type: string
          example: bar